# Email API Configuration
# Set this to your backend API endpoint that handles email sending
VITE_EMAIL_API_ENDPOINT=/api/send-demo-request

# Alternative: If using a third-party service directly (not recommended for production)
# VITE_EMAIL_API_ENDPOINT=https://your-backend-api.com/send-demo-request
